# skogtasks

Task management for SkogAI, split out into its own repository.

Entry point for context is `SKOGAI.md` (the project router). Claude Code
auto-loads `CLAUDE.md`, which routes to `SKOGAI.md`.
