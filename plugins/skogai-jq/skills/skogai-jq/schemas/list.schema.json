{
  "$id": "https://skog.ai/schemas/list.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Append-only checklist (.list)",
  "description": "One item per row, append-only. Serializes to `- [ ] <message>` / `- [x] <message>`. Each item carries a source path, optional parent action, and optional links for traceability.",
  "type": "array",
  "items": {
    "$ref": "defs.schema.json#/$defs/listEntry"
  }
}
